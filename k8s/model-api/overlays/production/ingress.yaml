apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: model-api
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: model-api
                port:
                  number: 8000
